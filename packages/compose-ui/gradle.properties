kotlin.code.style=official
android.useAndroidX=true
kotlin.mpp.androidSourceSetLayoutVersion=2
org.gradle.jvmargs=-Xmx2048m

# Bump this to cut a release (kept out of build.gradle.kts so the publish
# workflow can grep it the same way the npm workflow reads package.json).
version=0.1.2
